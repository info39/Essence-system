# ESSENCE – Studio OS (Static)

This is a single-file static dashboard (React via CDN + Babel) meant to be deployed as-is on Vercel.

## Structure
- index.html
- assets/essence-logo.png

## Notes
- `apiKey` is client-side in this version. For production, move AI calls to a Vercel serverless function and store the key in environment variables.
